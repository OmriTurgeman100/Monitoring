version: '3.8'

services:

  grafana:
    image: grafana/grafana
    ports: 
      - '4000:3000'
    volumes:
      - grafana_data:/var/lib/grafana

  redis:
    image: redis:trixie
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  grafana_data:
  redis_data: